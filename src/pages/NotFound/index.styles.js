import styled from "styled-components";
import { Main } from "../../theme/theme";

const UnknownPage = styled(Main)`
  display: flex;
  flex-direction: column;
  align-items: center;
`;

export { UnknownPage };
